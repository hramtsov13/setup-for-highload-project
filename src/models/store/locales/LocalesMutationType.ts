//LocalesMutationType — это пространство имён, в котором сгруппированы и экспортируются
//имена констант для типов мутаций (по аналогии с ItemsMutationType из девятой главы 9):
// группируем константы в пространстве имён

// eslint-disable-next-line @typescript-eslint/no-namespace
export namespace LocalesMutationType {
  export const selectLocale = 'selectLocale';
  // здесь добавляются новые мутации в модуле состояний локалей
}
